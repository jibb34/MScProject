# MySQL environment variables for the development database.
# Adjust these values as needed before starting the container.
MYSQL_ROOT_PASSWORD=toor
MYSQL_DATABASE=routeseg
MYSQL_USER=routeseg_user
MYSQL_PASSWORD=changeme-user

# Note: The following variables are used by docker‑compose.yml to mount
# host directories.  They do not affect the MySQL server configuration
# directly but allow the compose file to be self‑contained.  You can
# customise the host paths here if you prefer to persist data and
# initialization scripts somewhere else.
HOST_DB_DIR=./mysql-data
HOST_INIT_DIR=./init
HOST_CONFIG=./my.cnf
